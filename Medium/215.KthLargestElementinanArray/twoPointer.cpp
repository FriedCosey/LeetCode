class Solution {
public:
    int helper(vector<int>& nums, int pos, int head, int end, int left, int right){
        int mid = left + (right - left) / 2;
		while(left <= right){
            
		}
        
    }
    int findKthLargest(vector<int>& nums, int k) {
        helper(nums, nums.size() - k, 0, nums.size() - 1);
    }
};
